#pragma once

class QApplication;

namespace viki {

// Applies VikiCAD's dark theme: Fusion style + dark palette + styling for
// docks, tables, buttons and the command bar.
void applyDarkTheme(QApplication& app);

} // namespace viki
